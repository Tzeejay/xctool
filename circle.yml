

machine:
  xcode:
    version: 7.3.1
  environment:
    XCODE_SCHEME: xctool

test:
  override:
    - set -o pipefail && xcodebuild -workspace xctool.xcworkspace -scheme xctool clean build -verbose | tee $CIRCLE_ARTIFACTS/xcode_raw.log | xcpretty --color --utf

